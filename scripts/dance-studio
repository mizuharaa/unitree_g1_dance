#!/usr/bin/env bash
# Launch the G1 Dance Studio desktop app (pywebview window + local engine).
set -e
source "$HOME/miniconda3/etc/profile.d/conda.sh"
conda activate g1dance
exec python "$HOME/g1-dance/ui/desktop.py" "$@"
