# App quality audit — confirmed findings (2026-07-03)

39-agent adversarial audit (deploy safety path excluded — separately reviewed). 29 confirmed, 5 dismissed of 34 raised.

Split: **BUGS/ROBUSTNESS** (fix now) vs **PRODUCT FEATURES** (backlog).

## [HIGH][FIX] Vet no-floorwork pelvis gate + foot-skate stance test use absolute z thresholds against an un-grounded floor; nothing grounds the motion before the vet/window gate
- **Where:** `pipeline/vet_motion.py:71-74, :97; pipeline/find_window.py:22; pipeline/stages/local_motion.py:88-116; pipeline/retarget_gvhmr.py:51; pipeline/prep_motion.py:64-110`  (safety-gate-correctness)
- **Why real:** Every technical premise checks out on disk. vet_motion.py:71 compares raw root-z (`m[:,2].min()`) to an absolute MIN_PELVIS_HEIGHT_M=0.35 for HARD-3 (no floorwork); vet_motion.py:97 gates stance on absolute `fpos[:-1,:,2] < FOOT_CONTACT_HEIGHT=0.045`. Both are only meaningful if the floor is at z=0.

The gate runs on the raw motion with no grounding first. RetargetStage.run (local_motion.py:88-116) does window -> vet -> preview, and the only spatial normalization it performs is `seg[:,0:2] -= se
- **Fix:** Ground-reference the motion before any absolute-z test. Factor prep_motion._min_height_fk into a shared helper and, at the top of vet_motion.main (and longest_window), FK the trajectory, compute min robot-geom z, and subtract it from column 2 before evaluating HARD-3 / stance / the window z_ok mask. Equivalently, run the vet+window gate on the ground-corrected CSV. Do not compare an un-normalized 

## [HIGH][FIX] Video upload is unbounded and copied twice — a large file exhausts the operator laptop's disk
- **Where:** `ui/server.py:156-167 (create_job_upload), ui/server.py:136-143 (_create_job)`  (reliability)
- **Why real:** Confirmed reachable in real code; no existing guard. create_job_upload (server.py:164-165) streams the entire UploadFile to data/videos via shutil.copyfileobj with no byte count or size cap, then _create_job (server.py:140) does shutil.copyfile AGAIN into job.dir/input.mp4 — so a bad file consumes disk twice (and UploadFile's own spool file adds a transient third) before any validation. The only size-aware code, video_probe.validate(), is called at pipeline/stages/local_motion.py:66 on the alrea
- **Fix:** In create_job_upload, enforce a max upload size by counting bytes during the copyfileobj loop (chunked read) and abort+unlink the temp file on overflow; check free disk space (shutil.disk_usage) before accepting. Run video_probe.validate() on the single uploaded temp file BEFORE copying it into the job dir, and avoid the second full copy — shutil.move (rename within the same filesystem) or referen

## [HIGH][FIX] One corrupt/truncated job.json bricks app startup and the whole job list
- **Where:** `pipeline/store.py:101-117, ui/server.py:71-93`  (robustness / crash-recovery)
- **Why real:** Verified against real code. The core failure chain is exactly as described and has NO existing guard:

- store.py:102 load_job() does json.loads((.../job.json).read_text()) with zero error handling.
- store.py:112-117 list_jobs() calls load_job() for every dir containing job.json inside a plain for-loop; one raise aborts the whole loop and propagates out of list_jobs().
- server.py:74 _reconcile_jobs() iterates store.list_jobs() with no try/except, and is called SYNCHRONOUSLY at server.py:91 ins
- **Fix:** Two independent defenses, both needed: (1) Read-side resilience — wrap load_job() body in try/except in list_jobs() and _reconcile_jobs() so a single unparseable job.json is skipped/quarantined (log it, surface a 'corrupt' entry in the UI) instead of aborting iteration; and wrap the entire startup handler body so _reconcile_jobs and seed_initial_dances can never prevent the worker thread from star

## [HIGH][FEATURE] No audio/music handling: soundtrack is never stored, and the show deploy has no start-cue sync procedure
- **Where:** `pipeline/shows.py:109-131; ui/server.py:140,156-167`  (product-gap)
- **Why real:** Verified against real code. The Dance dataclass (shows.py:109-131) carries motion_csv, policy_path, preview, vet, sim_exam, repeatability, incident, notes — and NO music_file/cue/offset/latency field. Input copying (actually ui/server.py:140, not the finding's cited store.py:136-143 which is out of range since store.py is only 118 lines) does `shutil.copyfile(src, ... "input.mp4")` for pose extraction only; the audio track is never demuxed or persisted. A whole-tree grep for audio|music|soundtra
- **Fix:** At minimum, demux and persist the source audio per dance (data/dances/<id>/audio.*) at ingest and surface a repeatable 'press play + deploy together' countdown in Show mode so the operator has a consistent sync procedure. Add music_file plus a measured deploy→first-motion latency offset to the Dance record. Full solution: emit a single synchronized start cue that begins music and policy together, 

## [HIGH][FIX] No app path to attach a trained policy to an already-registered dance — the register-first / seeded-dance workflow dead-ends before show-ready
- **Where:** `ui/server.py:314-368; pipeline/shows.py:191-192,267-272,419-467; pipeline/stages/local_motion.py:147-165`  (workflow-gap)
- **Why real:** Verified against the real code (main tree, not the .claude/worktrees copies). policy_path is settable in exactly one place: register_dance / POST /api/dances at creation (server.py:331-334). The full dance route set is GET list, GET detail, POST create, POST /promote, POST /sim-runs (server.py:314-368) — no PATCH/PUT/DELETE. new_dance (shows.py:143) only takes fields at construction and nothing mutates policy_path afterward (grep of policy_path across pipeline/ + ui/ finds only the dataclass def
- **Fix:** 

## [HIGH][FIX] No export/backup/restore of the dance library — a single laptop failure loses weeks of paid training
- **Where:** `ui/server.py (27 routes, none for library export/import/backup); pipeline/config.py:5-16; pipeline/shows.py:37-38; pipeline/stages/local_motion.py:161`  (reliability / disaster-recovery)
- **Why real:** Confirmed in real code, no missed guard. All durable state lives only under data/ on one no-GPU laptop: jobs (config.py:6 JOBS_DIR), dances (shows.py:37 DANCES_DIR=DATA_DIR/'dances'), shows (shows.py:38), exam verdicts (sim_exam.py:689 data/exports/). ui/server.py exposes 27 routes (grep of @app decorators) and none exports/imports/backs up a dance — the only download path is /api/previews via FileResponse. The 'export' in STAGE_ORDER is a per-job pipeline stage: class ExportStage (local_motion.
- **Fix:** Add an export endpoint that bundles a show-ready dance (retargeted motion, trained policy + training metadata, sim-exam verdict, audio, and dance.json metadata) into a single self-contained archive with paths rewritten to be relocatable, plus a matching import/restore endpoint that registers the bundle into data/dances on a spare laptop. This makes a dance backable-up off-machine and portable, whi

## [MEDIUM][FIX] longest_window() is greedy and does not return the longest valid window
- **Where:** `pipeline/find_window.py:20-38`  (correctness)
- **Why real:** Verified by tracing the exact repro. In longest_window, after a window ends because frame end+1 exceeds MAX_EXCURSION_M relative to xy[start], line 37 sets start=end+1, permanently skipping every frame between old start+1 and end as a candidate start. Because the excursion bound is measured relative to the START frame (not a pairwise diameter), a later start can reach further, so skipping intermediate starts provably misses longer windows. Repro x=[0,1.0,1.4,1.6,1.7,1.8,1.9], y=0, all z_ok: gree
- **Fix:** Evaluate every candidate start, not just break points: advance start by one frame when the excursion bound breaks (or use a monotonic-deque approach), rather than jumping start=end+1. Note the constraint is relative to the moving start frame, so a naive two-pointer is not automatically O(n); per-start scanning restores correctness. Verify against the numeric repro (should return frames 1..6 = 6 fr

## [MEDIUM][FIX] Resolution/aspect advisory only fires when BOTH dimensions are small; no hard reject for extreme aspect ratios or zero dimensions
- **Where:** `pipeline/video_probe.py:67`  (input-validation)
- **Why real:** Verified against the real code. The guard `if min(width, height) and height < ADVISORY_MIN_HEIGHT and width < 1280` uses logical AND, so it only fires when BOTH height<720 AND width<1280. The reviewer's truth table is correct: 1920x400 (400<720 T, 1920<1280 F → False), 500x2000 (2000<720 F → False), 4000x100 (4000<1280 F → False) all produce no advisory, and min()==0 short-circuits 0x0 to False. Confirmed via grep that validate() is the ONLY intake guard for video geometry, and via local_motion.
- **Fix:** Split the check into two: advise when EITHER dimension is below threshold (height<720 OR width<1280), and — more importantly, since advisories are non-blocking — add a HARD reject (raise RuntimeError like the duration checks) for degenerate/extreme geometry: width==0 or height==0, and aspect ratio outside a sane band (e.g. ~0.4..2.5), so unusable footage fails locally in seconds instead of consumi

## [MEDIUM][FIX] Motion CSVs loaded without shape validation in the vet/retarget path — malformed files crash with cryptic tracebacks / HTTP 500 instead of a clear operator message
- **Where:** `pipeline/stages/local_motion.py:89, pipeline/vet_motion.py:42, pipeline/find_window.py:21`  (robustness / operator-facing error handling)
- **Why real:** Verified against the real code. local_motion.py:89 does `m = np.loadtxt(csv, delimiter=",")` then `longest_window(m)`, which slices `m[:, 0:2]`/`m[:, 2]` (find_window.py:21-22) with no shape or finite check; a single-column CSV loads 1-D and raises IndexError, a header row or non-numeric cell makes np.loadtxt itself raise ValueError, and ragged rows raise ValueError. vet_motion.py:42-50 likewise loads and immediately does `qpos[:,3]=m[:,6]` (IndexError for <7 cols) and the jnt_range broadcast at
- **Fix:** Add a shared loader (e.g. in find_window.py or a small io helper) that runs np.loadtxt, coerces to 2-D, and asserts exactly 36 columns and all-finite values, raising a human-readable RuntimeError ('this file isn't a 36-column G1 motion CSV — got N columns / header row / non-numeric data'). Use it at local_motion.RetargetStage:89, vet_motion.py:42, and find_window.longest_window so Studio shows a c

## [MEDIUM][FIX] Intake gate has no signal for multi-person, subject-leaving-frame, occlusion, or near-static footage — bad clips silently consume a paid cloud cycle and near-static motion passes the vet gate
- **Where:** `pipeline/video_probe.py:42-83; pipeline/stages/local_motion.py:52-73; pipeline/vet_motion.py:36-120`  (validation-gap)
- **Why real:** Verified against real code, no guard was missed. video_probe.validate() (42-83) checks only container duration (hard), resolution (advisory), and VFR delta (advisory) — zero pixel/pose/person analysis. ExtractStage.run() (local_motion.py:59-73) calls validate(), logs advisories, logs "video ok", then raises StageBlocked(CLOUD_MSG); there is no local person-count or motion-energy pre-check, so any readable 15s-240s clip passes intake and triggers the paid GreenNode extract+train cycle. I also ver
- **Fix:** Add a fast local intake pre-check before StageBlocked in ExtractStage (e.g. sample N frames, run a lightweight person detector): reject/advise when the clip has >1 dominant subject, when the subject's in-frame ratio drops below a floor for a stretch, or when frame-to-frame motion energy is below a floor (static clip). At minimum surface these as intake UI advisories so a non-technical operator get

## [MEDIUM][FIX] Post-show outcome buttons fire irreversibly with no confirmation — one misclick on 'Incident' closes the show and demotes a show-ready dance
- **Where:** `ui/static/app.js:406-418 / pipeline/shows.py:385-414`  (ux-safety / data-integrity)
- **Why real:** Verified against the real code; every load-bearing claim holds and no existing guard was missed. app.js:407 binds btn.onclick to POST /outcome directly with zero confirmation for all three buttons — no confirm(), no dialog. index.html:146-148 places 'Clean run', 'Aborted', 'Incident' side by side, all with class="outcome"; style.css:142 gives them identical neutral styling and none gets the existing `danger` class, so the destructive options are visually indistinguishable from the safe one. show
- **Fix:** Gate 'aborted'/'incident' behind a confirmation step in app.js (e.g. confirm() or a modal: "This marks the dance not-ready and requires re-verification. Continue?") and give those buttons the `danger` class to visually separate them from 'Clean run'. Optionally allow a short reversible window before shows.py:397 commits closed=True, or add a supervisor-only correct-outcome path so a mis-recorded i

## [MEDIUM][FIX] Preview video fails silently in Show mode — no onerror handler, unserved/dangling preview paths show a blank player with no message
- **Where:** `ui/static/app.js:308-316 (openDance) and app.js:122-127 (playPreview)`  (error-handling / operator-UX)
- **Why real:** Verified against real code. (1) No onerror/error handler exists anywhere in app.js (grep is empty); openDance line 316 does `if (d.preview){ v.src=d.preview; v.hidden=false; }` and playPreview line 124 does `v.src=url` — both set the source with no error handling or fallback. (2) The preview value can genuinely be an unserved path: register_dance (server.py:324-334) copies payload['preview'] verbatim with no validation, and shows.py:119 documents it may be a project-relative filesystem path; the
- **Fix:** Add a `video.onerror` handler in both openDance and playPreview that unhides a visible message ('Preview unavailable for this dance') and hides/greys the player, so 'broken' is distinguishable from 'none'. Server-side: validate/normalize Dance.preview to a served /previews URL at registration time (reject or convert raw filesystem paths), and prune dangling job-preview symlinks (this also prevents

## [MEDIUM][FIX] No connection-loss / engine-down indication in Show mode; operator sees stale data with no warning
- **Where:** `ui/static/app.js:445-447 (studio-gated polling), app.js:5-9 (api helper), ui/static/index.html:37-38 & 114 (cloud dot lives only in studio-main)`  (reliability)
- **Why real:** Verified in real code. The only periodic refresh (app.js:446) is gated on `!$('#studio-main').hidden`, so in Show mode no polling runs. The cloud status dot (#cloud-dot/#cloud-status) is inside #studio-main (index.html:37-38), hidden in Show mode. The one unconditional interval, refreshCloud every 30s (app.js:448), writes to those hidden elements and reflects the GPU box, not the local FastAPI engine, and rejects silently (unhandled) if the engine is down. The shared header (index.html:9-16) and
- **Fix:** Add a persistent health indicator to the shared header (index.html:9-16) so it shows in both modes. Poll /api/health on a mode-independent interval (not gated on #studio-main), and on rejection show a clear Show-mode banner like 'App engine not responding — do not start a show, contact the team.' Wrap the setInterval callbacks so rejections are caught and surfaced instead of silently swallowed, an

## [MEDIUM][FIX] Show deploy-dialog leaks its 'show' target when cancelled, misrouting the next Studio deploy to the show endpoint
- **Where:** `ui/static/app.js:161 (deploy-cancel), 156-159 (studio open), 381-404 (shared confirm)`  (state-management / UI correctness)
- **Why real:** Confirmed against the real code. `#show-deploy` (line 386) sets `deploy-confirm.dataset.target='show'`. The combined confirm handler only clears that flag on the successful-confirm branch (line 392). `#deploy-cancel` (line 161) is just `close()` and never clears it; the native ESC/`close` event bypasses the button entirely and also leaves it set. The Studio open path `#deploy-btn` (156-159) only clears the phrase, not `dataset.target`. `setMode` (265-273) toggles `hidden` classes with no page re
- **Fix:** Clear `deploy-confirm.dataset.target` in the `#deploy-cancel` handler AND at the start of the studio `#deploy-btn` open handler (and ideally on the dialog's `close`/`cancel` event to also cover ESC). Better: stop sharing one <dialog> across modes — give Show mode its own deploy dialog and confirm handler so cross-mode state cannot leak. Also route studio-vs-show results to the panel that is actual

## [MEDIUM][FIX] 30s cloud auto-refresh wipes connection fields the operator is typing
- **Where:** `ui/static/app.js:190-194,448`  (ux-correctness)
- **Why real:** Verified in real code. refreshCloud() (app.js:190-194) unconditionally assigns cfg values into #ssh-host/#ssh-port/#ssh-user/#ssh-key/#jup-url with no focus, dirty, or once-only guard. setInterval(refreshCloud, 30000) at app.js:448 runs the poll every 30s unconditionally — and unlike the jobs timer at 445-447 which IS guarded by `if (!$("#studio-main").hidden)`, the cloud timer has no guard at all, confirming the reviewer did not miss an existing protection. Config only persists via the manual "
- **Fix:** In refreshCloud, do not clobber inputs the user is editing: skip the value-writes when a cloud field (or the #cloud-details panel) has focus or is dirty, or populate the fields only once on initial load rather than on every 30s poll. Alternatively, gate the setInterval(refreshCloud,...) the way the jobs timer is gated and pause it while #cloud-details is open.

## [MEDIUM][FIX] Async action buttons never disabled during request, allowing double-submit of jobs and shows
- **Where:** `ui/static/app.js:61-74 (new-job), app.js:329-342 (show-start)`  (reliability / UX)
- **Why real:** Confirmed real for the two endpoints that lack idempotency, but the reviewer overreached on the deploy portion. Verified: the only `disabled` reference in app.js is line 58 (`deploy-btn.disabled=false`); there is no in-flight disable or finally-block guard anywhere. NEW-JOB (app.js:61-74): POST /api/jobs/upload -> create_job_upload (server.py:156) unconditionally calls _create_job per request; `#video-file` is cleared only AFTER the await, so during a multi-second upload the 'Create job' button 
- **Fix:** Disable the triggering button and show an in-progress state for the duration of each async action, re-enabling in a finally block — prioritize the upload submit (app.js:61) and start-show (app.js:329), where duplicates cost cloud compute or pollute show history. Cheap belt-and-suspenders: also dedup server-side (e.g. short-window guard keyed on file hash / dance_id+operator). Drop the 'duplicate d

## [MEDIUM][FIX] A failed pipeline stage produces no notification — the operator must be actively staring at the selected job to notice
- **Where:** `ui/static/app.js:41,45-46,445-447; app.js:22 + pipeline/store.py:62-67`  (ux-operational-reliability)
- **Why real:** Verified in real code. No notification mechanism exists (grep for toast/notif/beep/sound/audio/attention/blink/flash returns nothing). The 2.5s poll (app.js:445-447) silently calls refreshJobs()+showJob() with no attention cue. A failed stage surfaces only as a `state failed` badge inside #stages (app.js:41) plus the Retry button un-hiding (app.js:46) — both require the job to be SELECTED and the operator actively looking at the stages panel. Every alert() in the file is tied to a direct user ac
- **Fix:** On each poll, diff each job's stage states against the previous poll and raise a visible, dismissible toast/banner (optionally a short sound) when any stage transitions to failed or blocked. Add a top-level 'N jobs need attention' indicator, and differentiate failed/blocked jobs in the job list (app.js:22 currently shows the same 'waiting at:' text for running and failed) with a distinct color/lab

## [MEDIUM][FIX] Worker error path can swallow a job failure with no diagnostic trail (except: pass on recovery)
- **Where:** `ui/server.py:61-66`  (error-handling / observability)
- **Why real:** Verified against real code. The structural defect is real: _worker_loop's except branch recovers by calling store.load_job(job_id).log(...) wrapped in `except Exception: pass` (server.py:61-66). If the original throw was load_job itself (missing/corrupt job.json), the recovery load_job re-throws the identical error, hits the inner pass, and the failure is swallowed with nothing written anywhere and task_done() still called — the queued dance silently never progresses. That path exists exactly as
- **Fix:** On the worker error path, first write to a process-level fallback log under logs/ (or stderr) that does NOT depend on the job being loadable — capture job_id + traceback.format_exc() unconditionally. Only then attempt the per-job log/state update, and never use `except Exception: pass`: if the job can be loaded, set its current stage to 'failed' with the traceback and save so the operator sees it 

## [MEDIUM][FIX] Atomic writes have no fsync — power loss can leave zero-length state files
- **Where:** `pipeline/store.py:82-84, pipeline/shows.py:77-80`  (durability)
- **Why real:** Confirmed against the real code. store.py:82-84 save() and shows.py:77-80 _atomic_write() both do tmp.write_text(...) then os.replace(...) with no os.fsync on the file and no directory fsync (grep fsync = 0 hits in both files). os.replace gives rename atomicity (never a half-old/half-new inode) but no durability of the new file's data blocks across power loss. The crash chain is real: load_job (store.py:101-102), load_dance (shows.py:151-152) and load_show (shows.py:328-329) all call json.loads(
- **Fix:** In save()/_atomic_write: open tmp with a fd, write, flush, os.fsync(fd), close, os.replace(tmp, path), then open the containing directory and os.fsync its fd. Additionally wrap the json.loads in load_job/load_dance/load_show in try/except (JSONDecodeError, OSError) to surface a clean 'corrupt state' error instead of an unhandled crash.

## [MEDIUM][FIX] Cloud box password exposed in the process table via sshpass -p
- **Where:** `pipeline/cloud.py:92, 150`  (security)
- **Why real:** Verified against the real code and both halves hold. (1) Line 92: `_ssh_argv` unconditionally builds `["sshpass", "-p", s["password"], *argv]` when an SSH password is configured. The path is live and unguarded: run()->_run_ssh() (line 202)->_ssh_argv() (line 97)->subprocess.run(argv) (line 104). The plaintext GreenNode password is a genuine argv element, readable by any local user via `ps aux` or /proc/<pid>/cmdline for the lifetime of each ssh call — matching the shared/venue-laptop threat mode
- **Fix:** SSH: switch from `sshpass -p <pw>` to `sshpass -e`, and set the password via the custom env already built in _run_ssh (add env["SSHPASS"] = s["password"] near line 103) so it never appears in argv. Jupyter: drop the `?token={token}` query suffix at line 150 and rely solely on the existing `Authorization: token {token}` header at line 152.

## [MEDIUM][FIX] Uploaded/copied input videos accumulate with no size or disk-space guard
- **Where:** `ui/server.py:156-167, 136-143`  (resource-exhaustion / operational robustness)
- **Why real:** Verified against the real code and disk. server.py:164-165: create_job_upload is an `async def` route that streams the whole upload via synchronous shutil.copyfileobj into data/videos/upload-<ts>-<name> with NO max-size limit and NO shutil.disk_usage() free-space check. _create_job (line 140) then shutil.copyfile the source AGAIN into job.dir/input.mp4, so each upload leaves two full copies on disk. A codebase-wide grep for unlink/rmtree/disk_usage/max_size/content_length found zero guards or ga
- **Fix:** Enforce a max upload size and reject early (check Content-Length and/or cap bytes while streaming); call shutil.disk_usage() and refuse when free space is below a threshold before accepting or copying; delete data/videos/upload-* temp files once the job's input is copied into the job dir (or avoid the double-copy by pointing the job at the uploaded file / hardlinking). Consider running the copy in

## [MEDIUM][FIX] No monitoring/alerting — failed, blocked, and demoted states sit silently
- **Where:** `ui/server.py:114-116,222-242; pipeline/runner.py:23-61; pipeline/stages/local_motion.py:147-165; ui/static/app.js:15-46`  (observability / operator-safety UX)
- **Why real:** Every code fact in the finding checks out, and the reviewer missed no existing guard — in fact the real code is slightly worse than described.

1. runner.py:24 and 55-59 confirm: on a StageBlocked or Exception the stage state is set to "blocked"/"failed", saved to disk (job.save() at :61), and run_job returns (:25). There is no notification, retry-scheduling, or escalation of any kind — the state just persists in on-disk JSON.

2. local_motion.py:150-165 confirms TrainStage/VerifyStage/ExportSta
- **Fix:** Add a GET /api/summary (or expand /api/health) that aggregates counts of failed/blocked jobs, a worker-alive/last-activity heartbeat, and the last cloud-test result; persist _cloud_last to disk (e.g. alongside .secrets/cloud.json or in the store) so it survives restart. In the UI, add lightweight periodic polling (setInterval on refreshJobs) and mark failed/blocked jobs distinctly at the LIST leve

## [MEDIUM][FEATURE] No versioned policy artifact store / rollback across retrains (single policy_path + policy_sha256 slot)
- **Where:** `pipeline/shows.py:117-124 (Dance.policy_path / policy_sha256 single slot); corroborated at ui/server.py:324-334, shows.py:209-234, 250-278`  (data-model / operational-resilience)
- **Why real:** The underlying data-model observation is TRUE: the Dance dataclass has exactly one policy_path (shows.py:118) and one policy_sha256 (shows.py:124), and there is no versioned artifact store — nothing under data/dances/<id>/policies/<sha>/, no versions[] list. So the product cannot pin/roll back to a previously show-proven policy's bytes.

BUT the scenario's stated MECHANISM is overstated and not reachable as written in the real code, which is why I downgrade high->medium:

1. Nothing in the app e
- **Fix:** Adopt the reviewer's versioned-artifact approach but scope it to the real gap: on policy attachment, copy the .onnx into data/dances/<id>/policies/<sha>/ (immutable), and add a versions[] list on Dance recording {sha, train_job_id, exam_verdict_id, sim outcome, live-show outcomes} plus a live_version pointer. Provide an operator endpoint to pin/rollback live_version to any prior passed sha (re-val

## [MEDIUM][FEATURE] No set-list / multi-dance sequencing — a Show binds exactly one dance
- **Where:** `pipeline/shows.py:283-325`  (product-scope / feature-gap)
- **Why real:** The structural claim is TRUE and the reviewer did not miss a guard. Show (shows.py:283-294) has a single scalar dance_id/dance_name; new_show() (319-325) takes exactly one Dance; there is no Set/RunOrder entity anywhere (grep for set_list/run_order/setlist/encore returns nothing). The operator console (app.js:329-435) is a linear one-dance flow: #show-start posts one dance_id, renderChecklist walks CHECKLIST_KEYS once, one deploy, one outcome, then #show-back returns to the library. To run a 3-n
- **Fix:** 

## [LOW][FIX] Low-resolution advisory misses portrait and letterboxed videos (AND of both dimensions)
- **Where:** `pipeline/video_probe.py:67`  (correctness / input-validation)
- **Why real:** Confirmed against the real code. Line 67 is `if min(width, height) and height < ADVISORY_MIN_HEIGHT and width < 1280:`. The `min(width,height)` term is only a zero-guard; the effective advisory requires height<720 AND width<1280 — both dimensions small at once. Runtime check confirms: 480x852 portrait (poor 480px horizontal res) -> no advisory because height 852 is not <720; 1920x480 letterboxed -> no advisory because width 1920 is not <1280. Both are valid videos that pass the hard duration gat
- **Fix:** Warn when the smaller dimension is below 720 or pixel area is below 1280x720, e.g. `if min(width, height) and (min(width, height) < ADVISORY_MIN_HEIGHT or width*height < 1280*720):`. Keep the existing `min(width, height)` zero-guard so a missing/0 dimension does not spuriously trigger (0 < 720 is true).

## [LOW][FIX] Corrupt/duration-less videos are misreported to the operator as "0.0s — too short" instead of "unreadable/corrupt"
- **Where:** `pipeline/video_probe.py:51-56`  (error-handling / operator UX)
- **Why real:** Verified against the real code, no existing guard catches this. probe() (line 29) only rejects returncode != 0; validate() (line 48-49) only guards a missing video stream. Line 51-52's `float(format.duration or video.duration or 0)` collapses a missing/None duration to exactly 0, and line 53 `duration < MIN_SECONDS` then raises `f"video is {duration:.1f}s — too short..."` → literally "video is 0.0s — too short, the pipeline needs at least 15s". Nothing distinguishes "duration could not be parsed
- **Fix:** After parsing, treat a missing/zero duration as its own case before the length checks: if neither format.duration nor video stream duration is present/parseable, raise a distinct RuntimeError like "video appears corrupt or unreadable (no duration found) — try re-exporting the file" rather than falling through to the "too short" branch. Optionally add a lightweight decode confirmation (e.g. ffprobe

## [LOW][FIX] No cost/GPU-spend visibility in-app — accurate gap, but a business-intelligence enhancement, not a defect
- **Where:** `pipeline/cloud.py:207-240 (test_connection reports only name/mem/util; no timing or cost); confirmed absent project-wide`  (observability / unit-economics)
- **Why real:** The factual claim is correct and the scenario is genuinely reachable — the reviewer did not miss a guard. cloud.py's only reporting path, test_connection() (lines 211-240), surfaces GPU name/memory/utilization_pct and a busy flag; nothing about elapsed time or cost. A grep for cost|gpu.?hour|spend|hourly|billing|budget|monthly across all pipeline/ and ui/ .py/.js files returns ZERO matches, so there is no cost accounting anywhere else either — the owner truly cannot see what a dance cost or a mo
- **Fix:** Add an hourly-rate + billing-model field to cloud config (.secrets/cloud.json). In runner/store, derive train-stage GPU-hours from the existing started_at/finished_at (store.py:37-38) — and, more accurately for a rented notebook, track cumulative box-running hours since the last provision rather than only per-job wall-clock. Surface per-dance estimated cost and a running monthly total in the Studi

## [LOW][FEATURE] No rehearsal / dry-run mode distinct from a live paid performance
- **Where:** `pipeline/shows.py:385-414; ui/static/app.js:406-418`  (product-gap)
- **Why real:** The factual core is accurate and the reviewer did NOT miss an existing guard: there is genuinely no rehearsal/dry-run concept anywhere (grep for rehears|dry-run|practice across pipeline/ and ui/ returns nothing), Show has no rehearsal flag (shows.py:283-294), record_outcome only accepts clean|aborted|incident (shows.py:390), and on aborted|incident it resets consecutive_clean to 0, records an incident, and demotes show-ready->sim-verified (shows.py:401-413), reached directly by the outcome butto
- **Fix:** Optional, low priority. If desired, add a boolean rehearsal flag to Show (default False) set at create_show time, and in record_outcome skip the dance demotion/streak-reset block (shows.py:401-413) when show.rehearsal is True, while still closing/logging the show. Filter rehearsals out of list_shows()/refreshShowHistory by default. Separately (and more impactful than the rehearsal flag itself), co

## [LOW][FEATURE] No per-venue records; venue constraints hardcoded (fit-check redundant under fixed 2 m footprint)
- **Where:** `pipeline/shows.py:52-73, 283-294`  (product-gap)
- **Why real:** Code facts are accurate: the checklist hardcodes "hard flat floor, 2 m radius" (shows.py:60,71) as manual confirm steps, and the Show dataclass (shows.py:283-294) has no venue field — no Venue entity exists anywhere. BUT the finding's safety scenario is overstated and the reviewer missed the existing guard. The product is single-footprint by design: every deployable dance is hard-capped to the SAME 1.5 m excursion / 2 m radius. sim_exam.py:524 makes a PASS require max_exc <= EXCURSION_LIMIT_M (1
- **Fix:** Reframe: the automatic dance-vs-venue fit check is unnecessary (excursion is uniformly bounded to the one supported footprint). If desired for ops, add an optional Venue record (name, address, floor type, power, load-in notes) attached to a Show for logistics/records, and have the checklist surface the concrete required footprint ("venue must provide a 2 m-radius clear circle on hard flat ground")
