# Sim2real de-risking — verified findings (2026-07-04)

Multi-agent research, adversarially verified. 12 verified mitigations (of 29 raised). Corrected sources noted inline.
**Key framing: gantry-first already de-risks the FIRST run; several items are pre-GROUND, not pre-gantry.**

## [HIGH] observations / architecture — dual-policy fallback
- **Action:** Keep the dual-track plan, but fix three things before robot day: (1) Replace the source citation — the deepwiki unitree_rl_mjlab/7.2-g1 page describes a VELOCITY-TRACKING locomotion policy, not a motion-tracking policy; the correct authority is the BeyondMimic paper (arXiv 2508.08241). (2) The obs-restricted fallback must drop BOTH base_lin_vel AND the translational/global-position tracking terms (linear component of V_root and the translational part of the anchor error e_anchor), per BeyondMimic — dropping base_lin_vel alone still leaves position-estimator dependence. (3) Treat the fallback as a between-run ONNX swap (stop, reload, restart) that must first clear the SAME in-sim gates (Thriller 100%, noise+shove hold-out) with matched action_scale/armature — not a free 'cheap' artifact and not a live failover on a falling robot.
- **Source:** https://arxiv.org/pdf/2508.08241
- **Verifier note:** VERDICT: Core ACTION is SOUND and applicable to your stack, but the justification and confidence are partly WRONG — downgrade its self-label from "verified" to "likely" and fix the citation and the technical scope.

WHAT'S RIGHT (verified against BeyondMimic paper + whole_body_tracking repo):
- Path A is legitimate and matches how BeyondMimic actually deploys. I confirmed the BeyondMimic/whole_body_tracking ACTOR obs DOES include base_lin_vel with training noise Unoise(-0.5,+0.5) [tracking_env_cfg.py]. So your policy carrying base_lin_vel in the actor obs is STANDARD BeyondMimic, not a deviation — the intended real-robot answer is exactly "feed it from the onboard estimator." The deploy stack does ship one: DLIO/LiDAR-inertial odometry (Livox MID360 + torso IMU) plus a generalized-momentum observer + Kalman filter at 500 Hz, policy at 50 Hz. So "use the estimator it already ships + fine-

## [HIGH] Action / control latency modeling (retrain DR) + paired PD-gain DR
- **Action:** Confirm-with-corrections: add per-episode action-delay DR to the planned retrain, but change the range from U(0,40 ms) to U(20,40 ms) (1-2 steps at 50 Hz, never zero) to match ASAP's verified G1 recipe. Bundle PD-gain randomization into the SAME retrain (both ASAP and the FastTD3-15min G1 recipe pair delay+PD-gain DR; you flagged missing PD-gain DR separately). Implement as a per-env action ring-buffer in mjlab/Warp (no engine change). Measure real end-to-end Jetson delay (obs-in to torque-out) and center DR on it. REPLACE the fabricated source_url 2603.08619 (it is a balance-recovery paper, not latency DR) with arXiv 2502.01143 and 2512.01996; the 'verified' confidence was unjustified as written.
- **Source:** https://arxiv.org/abs/2502.01143
- **Verifier note:** ACTION is sound and applicable to your exact stack (mjlab G1 EDU, BeyondMimic-style tracking); latency DR is a confirmed standard for G1 whole-body tracking, not generic. But three fixes: (1) The cited source_url arXiv 2603.08619 is WRONG — it is a humanoid balance-recovery (capture-point) paper, not latency DR, so 'verified' is unjustified. Correct sources: ASAP (2502.01143) and FastTD3-15min (2512.01996), both of which list action/control delay in G1 DR. (2) Correct the range: ASAP uses Control delay U(20,40) ms (1-2 steps @50Hz), NOT U(0,40) — real robot delay is never zero; sampling zero wastes envs on an impossible regime. Your own '1-2 steps' estimate matches U(20,40) ms. Use per-episode constant (fixed within episode); avoid per-step random delay (non-physical jitter). (3) Do not ship delay DR alone — pair PD-gain randomization in the same retrain (both cited papers do; you separa

## [HIGH] Action low-pass / EMA smoothing (adversarial check of the flagged gap)
- **Action:** Keep the mitigation's primary directive: do NOT add a deploy-time-only EMA; rely on the confirmed ζ=2 overdamped impedance + action-rate penalty. Before robot day: (a) confirm r_smooth (action-rate, ideally + action-accel) weight is active and non-trivial; (b) fix the lag claim — an EMA's lag is τ/(1-τ) steps (τ=0.95≈19 steps≈380 ms, τ=0.8≈4 steps≈80 ms at 50 Hz), so any future EMA must be added in training too and its lag folded into latency DR; (c) implement generous torque/velocity saturation + slew-rate safety clamps that only trip on faults; (d) log joint vel/torque/action-rate on the gantry so buzz is diagnosed offline. Defer any 'add mild EMA + retrain' to a post-robot-day iteration, and prioritize adding action-latency DR (your real gap) over EMA work.
- **Source:** https://arxiv.org/html/2508.08241v1
- **Verifier note:** VERDICT: The core action is SOUND and specific to your stack — do NOT bolt on a deploy-time-only EMA. The paper is confirmed to use the exact impedance model you run (kp=I·ωn², kd=2·I·ζ·ωn, ζ=2 overdamped, ωn=10 Hz) and to achieve smoothness via the action-rate penalty r_smooth plus small per-joint action_scale (α=0.25·τmax/kp), with no output EMA/LPF on the tracking policy. The train/test-consistency argument (a filter present only at deploy is a sim2real mismatch) is a valid, well-established principle. Confidence: verified for the factual claims.\n\nBUT THREE CORRECTIONS/CAVEATS:\n\n1) MATERIAL MATH ERROR — the lag figure is wrong by ~20x. A one-pole EMA u_t=τ·u_{t-1}+(1-τ)·a_t has DC group delay ≈ τ/(1-τ) samples (equivalently time-constant -1/ln τ steps), NOT ~1 step. At τ=0.95 that is ~19 steps ≈ 380 ms at 50 Hz; at τ=0.8 it is ~4 steps ≈ 80 ms. So a deploy-only τ=0.95 EMA would be

## [CRITICAL] obs pipeline parity (sim2sim gate)
- **Action:** CONFIRM with corrections. The sim2sim obs-parity gate is sound, standard, high-value practice and directly applicable to mjlab + G1 EDU + motion_tracking_controller. Keep it as a hard gate for robot day, but fix three things: (1) Compare with a numerical TOLERANCE (e.g. atol 1e-4..1e-5), not bitwise/exact element-wise equality — float32-vs-float64, ONNX-vs-training-framework math, and any obs-noise scaling will otherwise trip false alarms. (2) The MotionCommand / reference-motion obs terms are phase/time-indexed, so the gate must align the reference timeline (phase index, loop wrap, dt) between the C++ deploy path and mjlab, not just the static joint name->index map, action_scale, and gravity-frame convention. (3) CRITICAL — the gate as written does NOT catch your #1 known gap (base_lin_vel unmeasurable on real G1). Feeding one recorded trajectory to both sides makes base_lin_vel match trivially because sim gives ground-truth base velocity; the parity check passes while the real deployment breaks. Resolve base_lin_vel explicitly BEFORE the gate: either confirm motion_tracking_controller/BeyondMimic omits base_lin_vel from its obs entirely (BeyondMimic obs is projected-gravity + base ang vel + joint pos/vel + last action + motion command — no base linear velocity; if so your policy's extra term will surface as a DIMENSION mismatch, which is the good outcome), or run the parity check with the actual state-estimator output substituted for base_lin_vel (estimator-in-the-loop), never sim ground truth.
- **Source:** https://github.com/HybridRobotics/motion_tracking_controller
- **Verifier note:** Action is correct and worth doing; not generic-wrong, not risky. Two source/framing fixes and one substantive gap. SOURCE: issue #45 is genuine but is a LOCOMOTION policy (not motion-tracking) and is UNRESOLVED — the phase-term cause is the reporter's unconfirmed hypothesis, so downgrade the citation-specific claim from 'verified' to 'likely'. The ACTION principle itself (parity gate) is verified best practice. ONNX-metadata joint-order/impedance claim CONFIRMED against HybridRobotics/motion_tracking_controller. BIGGEST FLAW: a naive record-and-replay parity test cannot expose the base_lin_vel gap (your stated #1 risk) because sim supplies ground-truth base velocity to both sides; it must be run with estimator-in-the-loop or used to confirm the deploy obs omits base_lin_vel via dimension mismatch. Also switch exact equality -> tolerance, and add reference-motion phase/time alignment to t

## [CRITICAL] initial-pose matching / policy activation
- **Action:** CONFIRM the mitigation and do it on the gantry — with three corrections. (1) Primary, lowest-effort fix: make frame-0 of the clip ≈ the standby default pose (default_joint_pos) so activation is pose-matched BY CONSTRUCTION — pick/trim Thriller to start from a static near-neutral standing frame; a clip that begins mid-dynamic-pose is the real hazard. (2) Add an explicit gate before R1+A: read joint telemetry, compute per-joint |q_current − q_frame0|, and only activate when max error is below a small threshold (e.g. a few deg); optionally interpolate standby→frame-0 over ~2-3 s first. (3) Also reset/confirm the reference motion phase = 0 at activation so the clip and robot start aligned in time. Keep B (damping) as the only stop — verified there is no torque-cut e-stop, so a bad activation cannot be hard-cut, which is exactly why pose-matching before R1+A matters on first deploy.
- **Source:** https://github.com/HybridRobotics/motion_tracking_controller
- **Verifier note:** VERDICT: Sound and directly applicable to your exact stack — do it — but fix the source, the confidence, and the mechanism framing.

WHAT'S RIGHT (confirmed against YOUR stack, not generic):
- motion_tracking_controller README confirms the flow: L1+A = Standby (joint POSITION control holding default_joint_pos), R1+A = activate policy, B = damping (the only stop). Standby holds the DEFAULT pose, and there is NO documented auto-ramp to the clip's first frame. So if frame-0 != default pose, activation is instantly out-of-distribution. Source: https://github.com/HybridRobotics/motion_tracking_controller
- whole_body_tracking (your training repo) uses initial-state randomization (RSI): the robot is reset TO the reference pose at a sampled frame. The policy is ONLY ever trained pose-matched to the reference — it has never seen a large frame-0 pose error, so this is genuinely OOD, not just 'sub

## [HIGH] action latency modeling
- **Action:** CONFIRM the technique, CORRECT the source, and DOWNGRADE the "must retrain before robot day" framing for a gantry-first test. Concretely: (1) Add a constant 1-step (20 ms at 50 Hz) action delay applied to a fixed fraction rho_delay=0.5 of training envs — this is a real, documented technique. (2) Also add/keep observation+actuator latency (0-20 ms sensor-to-actuator), since action delay alone does not cover the full loop. (3) Measure the real round-trip delay with a timestamp echo BEFORE trusting 20 ms — 18-30 ms is ~1-1.5 control steps, so 1 step is a reasonable central value but verify. (4) Do NOT let this block the gantry test: the gantry mechanically supports the robot, so latency-induced jitter is observable at near-zero fall risk. Run gantry day-1 as-is, watch for high-frequency oscillation; if present, it strongly motivates the retrain before any free-standing run.
- **Source:** https://arxiv.org/html/2506.15132v1
- **Verifier note:** VERDICT: Action is technically SOUND and applicable to your stack (mjlab/BeyondMimic action-delay is a framework-agnostic action-queue buffer), but the SOURCE IS MISATTRIBUTED and two claims need softening.

SOURCE CORRECTION (verified): The mitigation cites arxiv 2412.13196 = ExBody2. ExBody2 is where the "18-30 ms" number comes from — but ONLY as their measured real-hardware command delay in the deploy section; ExBody2 does NOT describe any training-time delay technique. The actual technique — "constant 1-step action delay on a fixed portion of envs, rho_delay=0.5" with the explicit rationale that random delay across all envs is worse and constant 1-step on half the envs "improves training speed and can prevent jittering" — is Booster Gym (arxiv 2506.15132). So confidence for the TECHNIQUE upgrades from "likely" to VERIFIED (exact rho_delay=0.5 documented); only the 20 ms value for YOU

## [HIGH] PD-gain / motor-strength randomization
- **Action:** CONFIRM the analysis but SPLIT it: for robot day, execute ONLY the no-retraining branch. (1) Highest-value, do-before-robot-day check: confirm the deploy-side low-level PD (kp/kd the Jetson motion_tracking_controller sends to each motor, i.e. the ONNX impedance metadata) EXACTLY equals the training values derived from your mjlab per-joint armature via kp=I*(2*pi*10)^2, kd=2*2*I*(2*pi*10) with I = reflected inertia = gear_ratio^2 * I_motor. A silent unit/gear/armature mismatch here is the single most likely fall cause and costs nothing to verify. Do this per-joint for all 29 DoF, including the 0.074-scale wrists. (2) Bench free-swing / step-response test on the gantry to validate the armature/reflected-inertia and torque-tracking assumption against the real actuator BEFORE any tracking run — good, keep it, but treat it as validation of the model you deploy, not as producing new numbers. (3) Do NOT add kp/kd (+/-15-25%) or motor-strength (+/-10-20%) randomization and deploy the same day: that is a retraining action, and because it changes the policy it INVALIDATES your current 100%/99% sim numbers and requires re-running the full held-out robustness suite. Schedule it as a next-iteration item, not a robot-day action.
- **Source:** https://arxiv.org/html/2508.08241v2
- **Verifier note:** VERDICT: Sound and applicable to your exact stack, but the framing needs correction on two points; do not treat it as a single "just add DR" action.

VERIFIED premises (BeyondMimic paper 2508.08241v2): kp=I*wn^2, kd=2*I*zeta*wn, zeta=2 (overdamped, chosen to absorb underestimated inertia), wn=10Hz, and DR restricted to friction/restitution + default joint positions + torso CoM. It explicitly does NOT randomize PD gains or motor strength and instead pairs small DR with accurate actuator modeling. So your setup (low gains, no PD DR) faithfully mirrors BeyondMimic, and the mitigation's diagnosis is correct.

CORRECTION 1 - "cheap and high-value" is misleading for retraining. Adding kp/kd or motor-strength randomization is cheap in GPU-hours but expensive in process: it produces a DIFFERENT policy, so your validated Thriller 100% / 99%-under-noise numbers no longer apply and must be re-earne

## [CRITICAL] Staged startup sequence (gantry-first) — button scheme + source corrected for motion_tracking_controller
- **Action:** CONFIRM the staged gantry-first de-risk STRATEGY, but REWRITE the checklist with the button scheme for YOUR actual stack (motion_tracking_controller), not unitree_rl_gym. Correct card: (1) hoist in zero-torque; (2) put robot in damping/debug via the remote combo FOR YOUR FIRMWARE (V1.0.2: L2+R2; V1.0.4+: L2+B — verify your version, do NOT hardcode L2+R2); (3) launch `ros2 launch motion_tracking_controller real.launch.py ...` — it boots into the STANDBY controller (joint-position hold); (4) press L1+A to (re)engage standby hold while fully suspended — THIS is your static-balance checkpoint, it is NOT the policy; make robot default pose == the clip's first frame so there's no jump at handoff; (5) lower hoist so feet kiss ground, weight on harness; (6) press R1+A to activate the policy — note this immediately plays the reference clip from t=0, you canNOT freeze mid-clip on a sub-segment, so pre-verify Thriller's opening ~2s is a stable stance (author a hold-in prefix if not); (7) keep an operator's thumb on B at all times = emergency stop = damping (gentle slump, NOT torque cut — harness must bear weight); (8) only after a clean static->opening hold, lower rope and run full motion. Each transition = abort-on-anomaly via B.
- **Source:** https://github.com/HybridRobotics/motion_tracking_controller
- **Verifier note:** VERDICT: CONFIRM the strategy, CORRECT the specifics + source. Downgrade confidence from 'verified' to 'likely' (mixed-source card).

WHAT'S SOUND (keep): gantry-first, feet-kissing-ground with weight on harness, static hold before dynamic tracking, checkpoint/abort at every transition, and never jumping straight to full Thriller. This matches BeyondMimic/Unitree practice and directly de-risks your untested-on-hardware policy. Keep it.

WHAT'S WRONG / RISKY (fix before robot day):
1) WRONG SOURCE FOR YOUR STACK. Cited URL is unitree_rl_gym; you deploy via motion_tracking_controller. Button schemes differ. Verified from the motion_tracking_controller README: L1+A = standby (joint-position hold), R1+A = activate policy, B = emergency stop (damping). The cited unitree_rl_gym README instead uses L2+R2=debug, Start=default pose, A(alone)=step-in-place, Select=exit — and does NOT contain 'R1+A

## [CRITICAL] base_lin_vel gap (gap #1) — obs is actor[0:3] from imu_lin_vel sensor
- **Action:** CONFIRM the mitigation's diagnosis and the two field checks, but correct three things before acting on it: (1) You trained in MJLAB, not BeyondMimic/Isaac. Masking is NOT a 'BeyondMimic flag' — it is a one-line edit deleting the base_lin_vel term from actor_terms in third_party/mjlab_mdp_ref/tracking_env_cfg.py (lines 61-65) + full retrain. The paper's 'linear components may be omitted' is a design PRINCIPLE that transfers; the mechanism is yours to implement. Masking also shrinks the proprio obs from 35->32/frame, so the motion_tracking_controller ONNX metadata / obs layout MUST be regenerated to match — not free. (2) FIX THE GANTRY TEST — it is contact-confounded and could give a false verdict. Legged base-velocity estimators (the generalized-momentum-observer + Kalman filter BeyondMimic describes, and legged_control2's estimator) fuse IMU with LEG KINEMATICS under a foot-in-contact / zero-velocity assumption. Fully suspended (feet off ground) VIOLATES that assumption: a working estimator can read garbage (false 'no-go'), and a broken one can still integrate a still-robot IMU to ~0 (false 'pass'). Validate instead with feet in FIRM CONTACT taking weight (standing on the gantry, lowered), and confirm what the estimator does when it detects no contact. (3) Reframe the no-go for gantry-FIRST: a bad base_lin_vel is a hard no-go for the first UNTETHERED/ground run, but the gantry run (feet loaded) is itself the safe test of whether the fed velocity keeps the policy in-distribution — that's the point of gantry-first with no torque e-stop. Also: 'retrain a variant' is a multi-hour cloud-GPU job + re-pass of the sim gate, NOT a robot-day action — train the masked (or noise/bias-robustified) fallback NOW so you arrive with BOTH ONNX policies and pick per the estimator check.
- **Source:** https://arxiv.org/html/2508.08241v3
- **Verifier note:** VERDICT: Sound diagnosis, applicable to your exact stack, with corrections. VERIFIED against sources: all four paper quotes are accurate — BeyondMimic obs includes robot root twist; 'when reliable state estimation is unavailable, the linear components may be omitted'; hardware full-state estimation at 500Hz via generalized-momentum observer + Kalman filter; LIO / drop-state-obs fallback for contact-rich motions (arxiv 2508.08241v3, Sec III-B & V-B). VERIFIED LOCALLY that this is a REAL gap for YOU: tracking_env_cfg.py puts base_lin_vel in the ACTOR group (deployed policy), not critic-only — obs[0:3], func=builtin_sensor sensor 'robot/imu_lin_vel'. In sim that is the TRUE body-frame velocity (velocimeter); a real IMU gives angular velocity + linear ACCELERATION, never velocity, so on hardware this channel is 100% estimator-dependent with NO direct-sensor fallback. Training only added Unoi

## [CRITICAL] base_lin_vel observation gap
- **Action:** CONFIRM (with two required corrections). The action is sound and applicable to your exact stack. Ship the No-State-Estimation retrain as the primary fix, BUT budget it as a full retrain + sim re-validation (not a deploy-day tweak), and keep the gantry estimator-logging step regardless.
- **Source:** https://arxiv.org/html/2508.08241v3
- **Verifier note:** VERDICT: SOUND and applicable to your stack (mjlab/unitree_rl_mjlab + HybridRobotics motion_tracking_controller + G1 EDU, gantry-first). Every load-bearing factual claim checks out verbatim; it correctly overturns your stated gap #1. Two corrections before you act on it.

WHAT IS VERIFIED (confidence: verified, all from BeyondMimic 2508.08241v3, the paper behind your deploy controller):
- "Full-state estimation is provided at 500 Hz using a low-level generalized momentum observer combined with a Kalman filter." So base_lin_vel is NOT unmeasurable on your path — your qiayuanl/motion_tracking_controller (legged_control2) feeds an ESTIMATED root twist to the policy. Your premise (gap #1 = unmeasurable) is wrong; the real gap is train-clean vs deploy-drifty velocity. The mitigation's reframing is correct.
- "when position drift compensation is unnecessary or reliable state estimation is unav

## [HIGH] actuator system identification (calibration)
- **Action:** CONFIRM (with corrections). Do run gantry-based actuator SysID, but: (1) fit the PLANT — per-joint armature, joint damping, frictionloss, AND a global action/latency delay term — while HOLDING kp/kd at the values you command to the G1 SDK (you set those gains, so they're known, not unknowns; jointly fitting them with armature/damping is partly degenerate/unidentifiable). (2) Do the differentiable fit in MJX (JAX), NOT in mjlab: mjlab runs MuJoCo-Warp, whose autodiff path is immature; identify in MJX then transfer the identified MJCF params into your mjlab model (they share the MuJoCo model format). There is NO turnkey 'built-in MuJoCo SysID utility' — you write the optimization loop (or reuse the community/Go2-style script). (3) Collect data with low-amplitude single-joint chirp/step sweeps on the gantry — this is a robot-day activity (you need the robot to log LowState), so treat it as robot-day-1 data collection + offline fit, not a strictly-before-robot-day task. (4) After baking identified values in, RE-VERIFY and likely RE-TRAIN if any identified value lands outside your current DR range; center DR on the identified nominal but KEEP the ranges wide — do not collapse them.
- **Source:** https://arxiv.org/abs/2604.10351
- **Verifier note:** VERIFIED CORE, source is real. arXiv:2604.10351 (Kovalev et al., 'Trajectory-based actuator identification via differentiable simulation', MuJoCo/MJX, Apr 2026) genuinely exists and supports the technique: backprop through a differentiable MuJoCo sim on encoder-only trajectories to fit actuator params, ~50% real position-error reduction, +46% locomotion travel / -75% rotational drift. A second independent source (arXiv:2508.04696, MJX, Mini-π biped) does the same but fits armature/frictionloss/damping while HOLDING PD FIXED — which is the safer default and directly contradicts this mitigation's 'fit kp/kd too.' So 'verified' is fair for the METHOD; the specific claim to fit kp/kd is NOT the standard practice and risks parameter degeneracy.\n\nAPPLICABILITY TO OUR STACK — mostly good, three real caveats: (a) Neither paper tests a humanoid or Unitree G1; both are quadruped/small-biped/isol

## [MEDIUM] mass / inertia / CoM measurement + torso-CoM domain randomization
- **Action:** Split the mitigation into two: (DO NOW, cheap, diagnostic) weigh the fully-kitted G1 EDU and measure/estimate the torso CoM shift from Inspire hands + battery + back-mounted compute, then compare against the CoM your policy was actually trained on to confirm the real robot sits inside your trained distribution — this is a pre-robot-day gate, not a day-1 fix by itself. (NEXT TRAINING RUN, requires retrain) add BeyondMimic's torso-CoM randomization (±2-3 cm, matching their base-CoM x U(-0.025,0.025) m / y,z U(-0.05,0.05) m) and correct the nominal mjlab base-link mass/CoM to the measured kitted values. Do NOT add all-link mass randomization as 'BeyondMimic' — it is not one of their terms and you already randomize mass. Do NOT add PD-gain randomization on this basis.
- **Source:** https://arxiv.org/html/2508.08241v2
- **Verifier note:** VERDICT: Core is SOUND and the factual claim is VERIFIED, but the action needs correction on two points and its timeline is mis-scoped. CONFIRMED (verified): BeyondMimic Sec III-G literally applies exactly three DR terms — ground friction, default joint positions (joint-offset calibration error), and torso CoM — so CoM being one of three is real and meaningful. Ranges: base CoM offset x U(-0.025,0.025) m, y/z U(-0.05,0.05) m, DoF bias U(-0.025,0.025) rad; the proposed ±2-3 cm torso CoM is well-aligned/conservative. CORRECTION 1 (likely): 'link-mass randomization ±10-15%' is mis-attributed — it is NOT a BeyondMimic term (their point is a minimal 3-term set) and you already randomize mass; the genuinely missing term is torso CoM only. CORRECTION 2 (verified logic): this is NOT a robot-day action — it requires a full retrain, since updating nominal mjlab CoM or adding CoM DR does not change
