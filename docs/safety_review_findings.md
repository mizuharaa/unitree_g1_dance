# Deploy safety review — confirmed findings (2026-07-03)

Adversarial multi-agent review (40 agents). 33 findings confirmed after refutation pass; 2 dismissed. NONE reached the robot — deploy path unused until robot day.
All must be closed before any hardware session. Severity order.

## [CRITICAL] Deploy gate trusts an unauthenticated, self-declared exam verdict — a fabricated or hand-edited exam_*.json authorizes ANY policy to the robot
- **Where:** `deploy/gen_config.py:38-53`
- **Scenario:** Verified against the real code. deploy/gen_config.py:38-53 (find_passing_exam) accepts the first exam_*.json whose fields match: schema=='sim_exam/v1', verdict=='pass' (top-level string, line 46), matching policy/motion sha256 (lines 47-48, 64-bit truncated prefixes from sha256() line 35), and push/repeatability merely present (lines 49-50, `is not None`). It never re-derives the verdict from nominal['pass'], push['pass'], repeatability['pass'], or repeatability['clean']==runs. gen_config then copies the untested policy into the bundle and stamps verdict['verdict'] into the manifest (line 97) 
- **Fix:** Stop treating the verdict JSON as an authorization token. (1) Re-derive the verdict in find_passing_exam from phase contents: require nominal['pass'] is True AND push['pass'] is True AND repeatability['pass'] is True AND repeatability['clean']==repeatability['runs']>=REQUIRED, rejecting empty/partial phase objects (a missing key or empty dict must fail, not pass). Do not read the top-level 'verdict' string as truth. (2) Authenticate the verdict: have sim_exam.py HMAC-sign the canonicalized verdict with a key the web app / general user processes cannot read, and have gen_config verify the MAC (

## [CRITICAL] 'Robot falls back to damping' on SIGKILL is an unverified assumption; kill_now.sh may leave stale full-authority torques
- **Where:** `deploy/kill_now.sh:16`
- **Scenario:** Confirmed against the real code. deploy/kill_now.sh:16 uses `docker kill` (SIGKILL) then `docker rm -f` — a hard kill that gives the controller zero opportunity to command a damping posture; line 17's claim "Robot low-level falls back to damping" is therefore an assertion about the G1 onboard low-level, not about anything the script does. I tried to refute via the "hardware e-stop is primary" mitigation (runbook abort ladder #1, line 84), but the repo's own docs undercut it: architecture.md:93 explicitly states that in the debug/rt-lowcmd mode they operate there is NO factory e-stop, only the 
- **Fix:** On robot day, empirically verify on the gantry (feet off ground) what the low-level does both when the controller is SIGKILLed and when lowcmd packets simply stop, and measure the command-loss-to-damping transition time. Prefer `docker stop` (SIGTERM → controller commands damping → clean exit) with SIGKILL only as a timed fallback after the controller has had a bounded window to damp. Add a mandatory show_mode_contracts.md / runbook clause requiring a confirmed low-level command-loss→damping watchdog (with recorded timeout) before any ground or show run, and until it is verified treat the hard

## [HIGH] Emergency software abort (kill_now.sh) refuses to run without the arming credential CONFIRMED_BY_HUMAN
- **Where:** `/home/alois/g1-dance/deploy/kill_now.sh:12`
- **Scenario:** Confirmed against the real code, not refuted. kill_now.sh:12 calls require_human (lib.sh:21-24) which die()s → exit 1 when CONFIRMED_BY_HUMAN != "alois". This runs BEFORE DRY_RUN=0 (line 14) and BEFORE the actual kill on line 16, so a shell lacking the exported var prints "ABORT: refusing..." and exits without stopping the container. The var is exported once at runbook step 0 (ROBOT_DAY_RUNBOOK.md:17) and lives only in that shell, so a fresh SSH session, a different tmux pane, a post-sleep laptop, or an accidental `unset` all break the abort — exactly the panic-grab-nearest-window scenario. No
- **Fix:** Remove the require_human gate from kill_now.sh. Killing the controller container drops the robot to damping (safety-positive) and needs no arming credential; ssh auth is key-based (BatchMode), so the env var is not functionally required. Make the script self-contained: hardcode DRY_RUN=0 and issue the docker kill unconditionally, never exit 1 on missing credential. Keep require_human on the arming/launch scripts only.

## [HIGH] Damping-hold guarantee is enforced only by an empty touch-marker and a manual human edit — no machine check that the controller starts in damping
- **Where:** `/home/alois/g1-dance/deploy/gen_config.py:109`
- **Scenario:** The core claim is factually correct and reachable. gen_config.py:109-126 emits start_controller_damping_hold.sh whose only gate is (1) existence of /bundle/LAUNCH_LINE_VERIFIED and (2) human-pasted launch-line content added on robot day (runbook step 3, ROBOT_DAY_RUNBOOK.md:43-44). NO code anywhere verifies that content holds damping: gen_config only writes the token inside a shell comment (line 123); 02_push_bundle.sh:27-38 checks sha256/exam verdict only; 10_gantry_test.sh:34 checks only that bundle.json exists, then docker-runs /bundle/start_controller_damping_hold.sh (lines 51-54) with no 
- **Fix:** Do not let a content-blind touch marker plus a human edit authorize motor motion. In 10_gantry_test.sh, before the docker run at lines 51-54, verify on PC2 that bundles/<dance>/start_controller_damping_hold.sh actually contains a required damping start-mode token (e.g. `pc2 "grep -q 'start_mode:=damping' .../start_controller_damping_hold.sh"`) AND that LAUNCH_LINE_VERIFIED exists — die otherwise. Have gen_config emit a wrapper that refuses unless the launch line carries the damping token the operator cannot omit, and replace the zero-byte LAUNCH_LINE_VERIFIED with a greppable/signed assertion 

## [HIGH] Fall detection is purely RELATIVE to the reference motion (no absolute upright/height/foot check), 45.8 deg tolerance stacked on already-tilted dance poses
- **Where:** `pipeline/sim_exam.py:385`
- **Scenario:** Confirmed against the real code and the real show motion. pipeline/sim_exam.py:385 defines the ONLY fall gate as `anchor_z_err > 0.25 OR ori_err > 0.8`, where anchor_z_err (line 375) = |torso_z - reference_torso_z| (relative to reference height, not floor) and ori_err (line 376) = geodesic(torso_quat, reference_torso_quat) (relative to reference orientation, not gravity). There is no gravity-relative upright check, no absolute base-height-above-floor check, and no foot/non-foot contact check anywhere in the closed-loop exam. A grep over pipeline/ and deploy/ shows the only contact/foot/floor l
- **Fix:** Add an ABSOLUTE fall gate independent of the reference in metrics(): fail if world-frame torso/base roll or pitch vs gravity exceeds a hard limit (e.g. 0.5 rad from vertical), if torso/base height drops below a fraction of nominal standing height (absolute, not delta-from-reference), or if any non-foot body geom contacts the floor (MuJoCo contact query). Keep anchor_z_err/ori_err as a separate, tighter tracking gate, and lower ANCHOR_ORI_FAIL below the 0.8 rad training-termination value so the safety exam has margin beyond what training already tolerated. Apply the same absolute gate in run_pu

## [HIGH] Nominal pass has NO tracking-quality, arm-motion, or joint-velocity floor -- wrong-choreography and high-speed arm flailing pass the gate
- **Where:** `pipeline/sim_exam.py:410`
- **Scenario:** Verified against the actual code; the exploit is reachable and no missed guard prevents it. metrics() (sim_exam.py:372-386) only fails on anchor_z_err>0.25 OR ori_err>0.8 (torso z-drift and torso tilt, 0.8 rad ~46deg is loose). run_nominal pass (line 410) = survived==T and max_exc<=1.5, where max_exc is ROOT xy excursion only. mean/max_anchor_pos_err and mean_joint_err_rad are computed and written to the verdict (lines 414-416) but NEVER thresholded — reporting-only, confirmed. No arm/hand kinematics are read anywhere: anchor is torso_link (lines 294-297); joint_err (line 377) is a np.mean ove
- **Fix:** Gate nominal pass on tracking quality: require mean/max anchor_pos_err and mean_joint_err below explicit thresholds derived from the vet motion (not just 'stayed upright'). Add per-joint velocity and torque-saturation checks to metrics(), and specifically bound end-effector (hand/wrist) speeds, failing run_nominal if any exceed a safe envelope. Since joint_err is a 29-joint mean that dilutes arm error, add per-body-group (upper-body/arm) error and velocity limits rather than a single averaged floor. Downstream (shows.py) should also assert the exam schema carries these thresholded fields rathe

## [HIGH] Exam gate over-optimistic: zero sensor/latency/DR noise + deterministic near-identical repeatability seeds make "works every time" meaningless
- **Where:** `pipeline/sim_exam.py:464`
- **Scenario:** Every technical claim verified against the code. sim_exam.py:464 reruns use jitter=0.02 rad on joints only; reset() (308-317) forces base pose/quat to exact nominal, zeros qvel, and applies no friction/mass/observation randomization; step() (362-369) computes PD torque from perfect, instantaneous, noise-free joint_pos()/joint_vel() with zero action/sensing/motor latency; the docstring (line 44) concedes "no observation noise replay". Default runs=5 (485), pass=clean==runs (472). Downstream, shows.py:41 REPEATABILITY_TARGET=3 and promote() (174-178) gates show-ready on consecutive_clean>=3, whi
- **Fix:** Add real domain randomization to the exam: randomize base position/orientation/velocity, ground friction, and payload/link mass across runs; inject additive observation noise matching real IMU/encoder specs; model at least one control-tick of action/sensing latency in step(). Critically, de-correlate the repeatability runs — right now hardcoded deterministic seeds make 3x-consecutive bit-identical, so it must randomize dynamics/noise per run to mean anything — and raise REPEATABILITY_TARGET (contract already flags 3->5). Until then, do not let show-ready's "works every time" label imply hardwa

## [HIGH] Push/PC2 integrity re-checks are self-referential; exam is never re-bound to the bundled policy, enabling a robot-day policy swap
- **Where:** `deploy/02_push_bundle.sh:27-38`
- **Scenario:** Verified against the real code. In deploy/02_push_bundle.sh:27-38 the embedded Python opens ONLY bundle.json: it asserts man["exam"]["verdict"]=="pass" (line 32, the manifest's own copied string) and compares recomputed file shas to man[part]["sha256"] (line 36, the manifest's own fields). It never opens exam_verdict.json, so the bundled verdict's policy_sha256/motion_sha256 (sim_exam.py:535/537) are never checked against what is shipped — the check is self-consistent by construction. gen_config.py:97 stores the verdict as a decoupled string copy, so after build the verdict file is provenance-
- **Fix:** In 02_push_bundle.sh, load bundles/<dance>/exam_verdict.json and assert exam["policy_sha256"]==manifest policy sha AND exam["motion_sha256"]==manifest motion sha, and re-derive pass from the phase contents (nominal/push/repeatability) rather than trusting man["exam"]["verdict"]. Have gen_config.py sign the whole bundle (a manifest-level sha over every file including exam_verdict.json and start_controller_damping_hold.sh). On PC2 in 10_gantry_test.sh, before docker run, recompute and verify that signature plus the policy/motion shas against exam_verdict.json, so a hand-modified or stale on-robo

## [HIGH] start_controller_damping_hold.sh and LAUNCH_LINE_VERIFIED are outside every integrity check
- **Where:** `deploy/gen_config.py:87-126`
- **Scenario:** Every factual claim checks out against the real code. gen_config.py:91-96 sha-pins only policy.onnx and motion.csv; the start script (written at 109-126) and controller.env (102-105) get no manifest hash. 02_push_bundle.sh:33 re-checks only ('policy','motion') — the start script is scp'd via the `"${BUNDLE}"/*` glob (line 47) but never verified. 10_gantry_test.sh:51-54 runs `/bundle/start_controller_damping_hold.sh` as the entrypoint of a `--privileged --network host` root container. The as-generated script is inert (exit 78 with only commented example lines), and ROBOT_DAY_RUNBOOK.md step 3 (
- **Fix:** Hash-pin every bundle file: add start_controller_damping_hold.sh and controller.env to the manifest (gen_config.py) and to the push-time verification loop (02_push_bundle.sh:33). Stop having a human hand-edit a privileged on-robot executable — parameterize the launch line as reviewed data in controller.env (also hashed) with a fixed start_mode=damping that the script asserts and cannot override; have the entrypoint fail closed if start_mode != damping. Make LAUNCH_LINE_VERIFIED carry the sha256 of the exact reviewed launch command plus who/when verified, and have the container entrypoint (and 

## [HIGH] A live incident/abort outcome does not demote the dance or reset repeatability
- **Where:** `ui/server.py:425-438`
- **Scenario:** Confirmed real after reading the actual code and attempting to refute it. record_outcome (ui/server.py:425-438) writes outcome onto the Show, sets closed=True, saves, and returns — it never loads or mutates the Dance. I traced every demotion path in the codebase: the ONLY function that demotes a dance (show-ready -> sim-verified) or resets consecutive_clean=0 is pipeline/shows.py:140-163 record_sim_run, and it triggers exclusively on a FAILED SIM EXAM, never on a real-world show outcome. There is no incident->demotion edge anywhere. I checked the show-creation path for a guard the reviewer mig
- **Fix:** In record_outcome, when result is "incident" (and arguably "aborted"), load the dance via shows.load_dance(show.dance_id) and force-demote: if status == "show-ready" set it to "sim-verified", set repeatability["consecutive_clean"] = 0, and record an incident reference on the dance (e.g. dance.incident = {"show_id": show.id, "at": ...}) so re-promotion requires a fresh deliberate re-examination through promote(). Additionally, block create_show / new_show for any dance carrying an unresolved incident flag until a human explicitly clears it.

## [HIGH] kill_now.sh emergency abort refuses to run without CONFIRMED_BY_HUMAN=alois in the current shell
- **Where:** `deploy/kill_now.sh:12`
- **Scenario:** Confirmed at the code level. deploy/kill_now.sh:12 calls require_human before any kill action, and lib.sh:21-24 die()s with exit 1 ("refusing: export CONFIRMED_BY_HUMAN=alois required") when the var is absent from the current shell. The var is a per-session export (ROBOT_DAY_RUNBOOK.md:17) unset at wrap (line 81), so in a fresh SSH/tmux pane, after the original terminal crashes/locks, or when a second person takes the keyboard, kill_now.sh exits without killing g1dance-controller while the robot keeps moving. The scenario is code-reachable and the design flaw is real: a safety-positive stop sh
- **Fix:** Remove require_human from kill_now.sh — stopping motion is always safe and must not share the arming interlock. Keep a physical e-stop as primary, but make the software kill fire from any shell with no env preconditions, and consider an always-running one-key listener so a crashed/locked original terminal does not disable it.

## [HIGH] Software abort (kill_now.sh) rides the same SSH link it's meant to backstop; link loss is fail-dangerous with no on-robot deadman
- **Where:** `deploy/kill_now.sh:16`
- **Scenario:** I tried to refute this and could not break the core mechanism — every technical claim is verified in the on-disk code:

1. kill_now.sh:16 issues the stop through pc2() (lib.sh:28-35): `ssh -o BatchMode=yes -o ConnectTimeout=5 unitree@192.168.123.164 "docker kill ..."`. Confirmed.
2. kill_now.sh:7 sets `set -euo pipefail`. If ssh fails (link down), it blocks up to ConnectTimeout=5 then returns non-zero, and the script aborts before the "kill issued" log at line 17. Confirmed.
3. Architecture makes the scenario reachable, not hypothetical: 10_gantry_test.sh:51-54 launches the controller as `dock
- **Fix:** Add an on-Jetson supervisor process (outside the SSH path) that holds an operator heartbeat and commands local damping the instant the heartbeat lapses, so link loss becomes fail-safe. Downgrade kill_now.sh in the abort ladder to a labeled convenience layer atop the hardware e-stop, and document in ROBOT_DAY_RUNBOOK.md that it is NOT usable when PC2 comms are degraded (the very case it appears to cover). Keep the hardware e-stop as the primary, as already intended.

## [HIGH] No automatic safe-state on policy NaN/limit violation — relies on a human spotting it in a scrolling log
- **Where:** `docs/ROBOT_DAY_RUNBOOK.md:58`
- **Scenario:** Confirmed, could not refute. Grep across the repo shows the ONLY clamps are pipeline/sim_exam.py:367 (np.clip on tau inside the MuJoCo sim — never runs on the robot) and pipeline/prep_motion.py:45 (offline motion-CSV preprocessing). No runtime NaN/limit guard exists on the real-robot control path. deploy/gen_config.py:109-125 emits start_controller_damping_hold.sh whose only contract is 'load policy -> HOLD DAMPING -> operator arms'; it refuses until LAUNCH_LINE_VERIFIED but then just execs the third-party qiayuanl/unitree:jazzy controller with no attestation of its non-finite-output behavior.
- **Fix:** Add a hard clause in docs/show_mode_contracts.md requiring the controller to force damping on any non-finite policy output, control-loop overrun, or missed tick, and to clamp targets to joint limits — independent of the operator. Make it a bundle-verified precondition (a checked field in deploy_bundle/v1 and a robot-day verification step), not a human 'ABORT IF' bullet. Note that limit-clamping alone does not trap NaN, so the finite-value check must precede any clip.

## [HIGH] Battery '30% floor' is documentation-only and never enforced; value is self-reported and unmonitored mid-show
- **Where:** `pipeline/shows.py:264`
- **Scenario:** Verified in real code. pipeline/shows.py:256-265: the battery step (kind="number") is validated only for bool-rejection, float-parseability, and 0<=value<=100 (line 264 is the sole bound). A value of 12.0 passes. The "Below 30% do not start a show" rule exists only as prose in the step detail (shows.py:57) and OPERATOR_MANUAL.md:22-23; nothing reads it programmatically. checklist_complete() (shows.py:212-219) only checks each step key is present, never re-inspects the stored battery value. The app deploy gate (ui/server.py:412-416) unlocks on checklist_complete() + typed "DEPLOY" and never rea
- **Fix:** Enforce a battery floor in complete_step for the battery step (reject value < a safety margin above 30%, not just 0-100), and ideally read SOC/voltage from the robot instead of trusting typed input. Add a mandatory mid-show battery/voltage watchdog on the Jetson that trips to damping when SOC/voltage crosses the measured actuation-margin threshold, with that threshold derived from the step-8 endurance data before any audience show. Also add an explicit robot-battery abort criterion to the ground/show steps of ROBOT_DAY_RUNBOOK.md (distinct from the e-stop remote battery).

## [HIGH] 'Hardware e-stop' is actually the Unitree wireless remote's B-damping command; OPERATOR_MANUAL claims a physical torque-cut that does not exist
- **Where:** `docs/OPERATOR_MANUAL.md:55`
- **Scenario:** I tried to refute this and could not; the repo's own engineering notes confirm it harder than the reviewer did.

What OPERATOR_MANUAL.md:55 claims: on a fall, "E-stop immediately (cuts motor torque)", and golden rule 1 (line 15) makes this handheld e-stop THE primary audience safeguard for the whole show.

What the code/docs actually establish the device is:
- docs/architecture.md:64 and :93 (Risk #2): the deploy path runs the robot in debug mode, and "Debug mode has **no factory e-stop** — only the controller's B-damping. Gantry is the real e-stop for every first run." The deploy joystick pro
- **Fix:** Before any audience show, resolve the contradiction physically, not just in prose: (1) Determine whether a real wired, power/torque-cutting kill exists on this tether-free G1. Per architecture.md:93 and research-findings.md:341/628 it does not — the only stop is the remote's B-damping. (2) If so, stop calling it a "hardware e-stop" and stop claiming it "cuts motor torque" (OPERATOR_MANUAL.md:55, ROBOT_DAY_RUNBOOK.md:8); describe it accurately as a wireless damping command with radio failure modes. (3) Provision an actual fail-safe for the show: either a wireless-e-stop receiver that trips a ba

## [MEDIUM] CONFIRMED_BY_HUMAN is a hardcoded, publicly-documented, inheritable string — it proves nothing about human presence
- **Where:** `?`
- **Scenario:** Mechanism is verified against real code and survives scrutiny: deploy/lib.sh:21-23 gates on a plain inheritable env var compared to the hardcoded string "alois", and that literal is documented in cleartext (lib.sh:6, deploy/README.md:16, docs/ROBOT_DAY_RUNBOOK.md:17 as `export CONFIRMED_BY_HUMAN=alois`, docs/deploy_kit_built.md:29). Persisting it in ~/.bashrc would silently satisfy interlock #1 in every future shell, including non-interactive ones, so one of the two claimed "independent interlocks" degrades to a no-op. That is a genuine defense-in-depth weakness worth fixing.

However, the fin
- **Fix:** Harden require_human but recognize it is not the sole last gate: (1) refuse when stdin/stdout are not a TTY (`[ -t 0 ] && [ -t 1 ]`) so persisted env vars cannot arm a non-interactive shell; (2) require a per-session freshness proof (short-TTL token file written by an explicit `arm` command with a timestamp, rejected if older than N minutes) instead of a static string; (3) stop printing the accepted literal value in lib.sh/README/RUNBOOK. Note the actuation script (10_gantry_test.sh) already enforces explicit flags + a TTY-typed "ROBOT IS SECURED" phrase, so this fix chiefly restores the inten

## [MEDIUM] Typed 'ROBOT IS SECURED' phrase reads from stdin with no TTY check — satisfiable by pipe/heredoc
- **Where:** `/home/alois/g1-dance/deploy/10_gantry_test.sh:40`
- **Scenario:** Verified against the real code. 10_gantry_test.sh:40 is `read -r -p "..." ANSWER` reading stdin, and no guard anywhere in the path adds a TTY check: require_human (lib.sh:21-24) only tests CONFIRMED_BY_HUMAN=alois; the arg loop (lines 17-25) sets GANTRY/ESTOP/DRY_RUN from argv; line 41 is a plain string compare. So `printf 'ROBOT IS SECURED\n' | CONFIRMED_BY_HUMAN=alois ./10_gantry_test.sh --dance thriller --gantry-confirmed --estop-confirmed --arm` satisfies all interlocks and reaches the docker run at line 51 with zero live keyboard attention. The exploit is genuinely reachable and the revie
- **Fix:** Add `[ -t 0 ] || die 'refusing: arming confirmation must be typed at an interactive terminal'` immediately before the read at line 40, and read from the controlling terminal explicitly (`read -r -p "..." ANSWER </dev/tty`) so a redirected stdin cannot supply the phrase. Optionally strengthen with a fresh random nonce the operator must retype so a static piped/history string cannot satisfy it. Note this is a defense-in-depth hardening: the downstream physical-remote start interlock still independently gates actual motion.

## [MEDIUM] --gantry-confirmed is a rubber-stamp flag: the only controller-start script is reused on the ground, forcing a false "feet off ground" assertion
- **Where:** `/home/alois/g1-dance/deploy/10_gantry_test.sh:29`
- **Scenario:** I tried to refute this and could not — there is no missing guard. Facts verified in the real code:

1. 10_gantry_test.sh header (line 2) and README.md (line 12) both state this is the ONLY script that starts the controller (`docker run ... start_controller_damping_hold.sh`, lines 51-54). grep across deploy/ confirms no other `docker run`/controller-start path exists, and no `--stage`/`--on-ground` flag exists.

2. The script hard-requires --gantry-confirmed at line 29 (`die "refusing: --gantry-confirmed not given (robot must hang, feet off ground)"`), and the interactive checklist hardcodes ga
- **Fix:** Split the controller-start into explicit stages: add a mandatory `--stage gantry|ground` (or an `--on-ground` flag) that (1) rewrites the interactive checklist to the true precondition for that state, (2) requires a distinct typed phrase per stage (e.g. "FEET OFF GROUND" vs "GROUND SAFETY LINE SET"), and (3) enforces stage-specific abort criteria. Never let one asserted precondition (feet-off) cover both feet-off and feet-on states. Also update runbook step 7 to name the exact ground invocation instead of leaving operators to reuse the gantry command.

## [MEDIUM] sim_exam nominal PASS ignores tracking error and ground contact and tolerates large lean/sag — a policy that stands still or half-collapses can certify as deployable
- **Where:** `/home/alois/g1-dance/pipeline/sim_exam.py:385`
- **Scenario:** Confirmed against the real code; not refuted. sim_exam.py:385 gates a per-tick failure only on anchor_z_err > 0.25 m OR ori_err > 0.8 rad. joint_err is computed (:377), stored (:383), averaged and reported as mean_joint_err_rad (:400/:416), but never appears in the failed flag (:385) or the nominal pass expression (:410) — it is pure telemetry, exactly as the finding states. Nominal pass = survived==T and max_exc <= 1.5 m (:410), where excursion (:378) is the robot's OWN root displacement from its start, so a stationary policy scores ~0 and passes that clause trivially. The exploit is in fact 
- **Fix:** In ExamEnv.metrics/run_nominal add hard nominal gates on joint tracking: reject if mean/max joint_err exceeds a fidelity threshold (the policy must actually perform the motion, not just balance) and gate on anchor_pos_err (horizontal), not just z. Tighten ANCHOR_Z_FAIL_M / ANCHOR_ORI_FAIL to sit with explicit margin BELOW the termination thresholds they currently equal, so a PASS implies distance from fall onset rather than arrival at it. Add a minimum foot-contact / non-collapse check. Fail-closed on any NaN per-tick metric (current comparisons treat NaN as not-failed). Optionally surface mea

## [MEDIUM] Bundle exam provenance not integrity-pinned; 02_push re-checks only policy/motion shas and a copied verdict string, never the exam file
- **Where:** `/home/alois/g1-dance/deploy/gen_config.py:97`
- **Scenario:** Verified against real code. gen_config.py:97 records the exam as {"file":"exam_verdict.json","verdict":verdict["verdict"]} — no sha256 of exam_verdict.json, only a copied verdict string. 02_push_bundle.sh:27-38 asserts man["exam"]["verdict"]=="pass" and re-hashes only policy.onnx and motion.csv against the manifest's OWN recorded shas; it never opens exam_verdict.json nor checks that the exam's policy_sha256/motion_sha256 cover the actual bytes. This is a self-consistency check, not a provenance check. I confirmed no downstream re-validation: 10_gantry_test.sh:34 only does `test -f bundle.json
- **Fix:** In gen_config, add sha256 of exam_verdict.json to the manifest. In 02_push_bundle.sh's python block, open the bundle's own exam_verdict.json and re-run find_passing_exam's full match (schema==sim_exam/v1, verdict==pass, both push+repeatability phases present, policy_sha256==bundle policy sha, motion_sha256==bundle motion sha) against it, aborting on any mismatch; also verify the exam file's recorded sha. Note this only closes the stale/honest-mistake path — to close the adversarial hand-forged-exam case, sim_exam must sign the verdict (e.g. HMAC/keypair) and gen_config/02_push must verify that

## [MEDIUM] sha-match pins only .onnx/.csv bytes, not the on-robot control law the exam validated
- **Where:** `pipeline/sim_exam.py:143`
- **Scenario:** Technically accurate and grounded in real code. sim_exam.py:167-186 reads kp/kd/default_pos/action_scale/joint-order/obs-order from ONNX metadata_props, the docstring (lines 15-45) explicitly labels them unverified "INTERFACE ASSUMPTIONS," and step() (364-366) builds the closed-loop law from them; act() (188-193) runs obs through sess.run with no semantic/order check. gen_config.find_passing_exam (38-53) and 02_push_bundle.sh integrity check (33-36) verify ONLY policy_sha256 + motion_sha256 + verdict==pass — nothing pins or asserts the qiayuanl/unitree:jazzy controller's live gains/joint-mappi
- **Fix:** Have gen_config.py record the exam's resolved control-law spec (kp, kd, default_joint_pos, action_scale, joint order, obs term/history layout, ONNX input dim) into bundle.json, and have the on-robot start_controller script assert its live config byte-matches that spec (and that the ONNX input dimension equals the obs layout) before it will leave damping hold — refuse otherwise. Resolve the INTERFACE ASSUMPTIONS in the docstring against the real mjlab/whole_body_tracking export so the exam and controller demonstrably read the same source of truth. This closes the automated gap that today only t

## [MEDIUM] verdict:'pass' (exit 0) is emitted when push and/or repeatability phases are skipped via --skip-push/--skip-repeat
- **Where:** `pipeline/sim_exam.py:543`
- **Scenario:** Confirmed at pipeline/sim_exam.py:543 and 551: with --skip-push --skip-repeat, push/repeat are None (lines 515/518), so the verdict expression `nominal["pass"] and (push is None or push["pass"]) and (repeat is None or repeat["pass"])` reduces to nominal["pass"], emitting top-level verdict="pass", writing push:null/repeatability:null, and returning exit 0. The exam has NO internal guard against this. I tried to refute via the interlocks: the only hardware-touching consumer, deploy/gen_config.py:49-50, does reject null push/repeatability, and deploy/02_push_bundle.sh:32 re-checks a manifest only
- **Fix:** Make the top-level verdict reflect phase completeness: set verdict="pass" only when nominal, push, and repeatability all ran and passed; if any phase was skipped (push is None or repeat is None), emit verdict="incomplete" (never "pass") and return a non-zero exit code (e.g. 2). This makes the exam's own printed VERDICT and process exit code honest instead of relying solely on gen_config's null-phase check to catch it downstream.

## [MEDIUM] Push test is a single weak horizontal COM shove during a narrow mid-motion window, and force is caller-tunable downward
- **Where:** `pipeline/sim_exam.py:434`
- **Scenario:** Every factual claim checks out against the real code. Line 434 builds force_vec = [cos, sin, 0.0]*force_n — purely horizontal, z=0. Line 368 applies it via data.xfrc_applied[self.torso_id, :3] = push, i.e. at the torso/anchor body only: no foot/ground perturbation, no downward component. Constants confirm PUSH_DURATION_S=0.1, so default 250 N = 25 N*s (~0.7 m/s on 35 kg). Line 424 samples push_ticks from arange(2*CTRL_HZ, T-3*CTRL_HZ), so the robot is never perturbed in the first 2 s (get-up/settle) or last 3 s (final pose) — the exact transitions likeliest to be unstable live. Line 446 scores
- **Fix:** Make the push suite adversarial and non-bypassable: (1) enforce a minimum force floor in code (e.g. clamp force_n to a realistic human-bump impulse floor inside run_push_test, and record min_force_n in the verdict); (2) have deploy/gen_config.py find_passing_exam and pipeline/shows.py reject any verdict whose push.force_n is below that floor rather than only checking push is not None; (3) add downward and foot/ground perturbations and simultaneous/multi-directional shoves; (4) time pushes to include the currently-excluded early get-up, late final-pose, and single-support/low-CoM phases; (5) re

## [MEDIUM] Repeatability/show-readiness badge trusts an arbitrary caller-supplied 'passed' bool, not tied to any sim_exam verdict/sha/phases
- **Where:** `ui/server.py:346`
- **Scenario:** The core technical claims are accurate against the real code. server.py:351 validates only `isinstance(payload.get("passed"), bool)` and never ingests a sim_exam/v1 verdict, never checks policy_sha256/motion_sha256, and never verifies push/repeatability phases ran. shows.record_sim_run (shows.py:145,152) increments repeatability.consecutive_clean and sets dance.sim_exam={"verdict":"pass"} purely from that bool. promote() (shows.py:171-178) grants show-ready on exactly `sim_exam.verdict=='pass' and consecutive_clean>=3`, both POST-controlled. The /promote endpoint is itself an unauthenticated l
- **Fix:** Have the sim-runs endpoint ingest the actual sim_exam/v1 verdict JSON (or its on-disk path/exam_id) instead of a bare bool: verify schema=='sim_exam/v1', verdict=='pass', push and repeatability non-null, and that verdict.policy_sha256/motion_sha256 match the sha256 of the dance's registered policy_path/motion_csv before crediting consecutive_clean or setting sim_exam.verdict. Reject POSTs whose sha does not match the dance under test. This aligns the show-readiness badge with the same evidence gen_config.py already requires, closing the divergence between the app badge and the hardware gate.

## [MEDIUM] 'show-ready' badge is forgeable and bound to a name, not a tested policy sha
- **Where:** `pipeline/shows.py:140-181`
- **Scenario:** All technical claims verified in the real code. pipeline/sim_exam.py has zero HTTP (grep for requests/urllib/http/POST is empty) — it computes a verdict dict (line 472) but never POSTs, so the documented producer does not exist. ui/server.py:346-356 accepts /api/dances/{id}/sim-runs with only isinstance(passed,bool) validation, no auth (no Depends/token/Authorization anywhere), bound to 127.0.0.1 (line 457). shows.py:140-163 record_sim_run increments repeatability.consecutive_clean purely from that boolean and sets sim_exam={'verdict':'pass'}. shows.py:171-178 promote() checks only the self-as
- **Fix:** Bind show-readiness to authenticated exam artifacts. The /api/dances/{id}/sim-runs endpoint (server.py:346) should accept only a verified sim_exam/v1 verdict (verify signature/provenance server-side, reject bare booleans) and record its policy_sha256 on the dance (add a policy_sha256 field to Dance). In promote() (shows.py:171-178), additionally require dance.policy_sha256 == sha256 of the file currently at policy_path AND that the passing verdict driving consecutive_clean was for that exact sha. This aligns the badge with the same sha-matched-verdict contract gen_config.py already enforces at

## [MEDIUM] Operator picks the deployed policy file by hand — nothing ties the app's show-ready dance to the bundled/pushed artifact
- **Where:** `docs/show_mode_contracts.md:56-59 (root cause: deploy/gen_config.py:56-72 + pipeline/shows.py:78-98,166-181)`
- **Scenario:** Confirmed against the real code, and the attempted refutation fails to close the specific gap. gen_config.py:56-72 drives entirely off free CLI paths: --dance is only an output-folder/manifest label that is never looked up, and it computes shas of whatever --policy/--motion files you hand it (line 71), matching them against on-disk exam JSONs via find_passing_exam (lines 38-53). It never consults pipeline/shows.py or any show-ready state. On the app side, the Dance dataclass (shows.py:78-98) stores only policy_path (a path string, :87) and a sim_exam summary with no sha (:152,157) — grep confi
- **Fix:** Introduce one full-file policy sha256 as artifact identity that flows end to end: dance registration stores policy_sha; record_sim_run/exam verdict carries and matches it; promote() to show-ready requires it; gen_config records the show-ready dance's reviewed sha in bundle.json; and a deploy step (02_push_bundle.sh or a preflight) cross-checks the bundle's policy sha equals the show-ready dance's reviewed sha before anything leaves the laptop. Add a PATCH endpoint so binding a policy to an existing dance goes through the API (re-verified) instead of editing dance.json out of band, closing the 

## [MEDIUM] Show-ready badge reachable with a policy never push/repeatability-tested (app ignores phase presence)
- **Where:** `pipeline/shows.py:140-181`
- **Scenario:** Verified against real code. sim_exam.py:543 yields verdict=="pass" when push/repeat are None (--skip-push --skip-repeat). server.py:351 only checks passed is a bool; no phase field is read. shows.py:140-163 record_sim_run counts consecutive_clean on passed alone and caches sim_exam={"verdict":"pass"} with no phase record. shows.py:171-178 promote checks only verdict=="pass" and consecutive_clean>=3. This directly contradicts docs/show_mode_contracts.md:133-134, which requires the badge to demand verdict=="pass" with BOTH push and repeatability phases present. The exploit (nominal-only exam x3 
- **Fix:** Carry phase presence through the sim-run contract and enforce it server-side. Add required push_present/repeat_present (or the full exam verdict) to the POST /api/dances/{id}/sim-runs payload; in record_sim_run only advance consecutive_clean when passed AND push_present AND repeat_present, and reject passed=true when either phase is null; re-assert both phases were present in promote() for the run that set sim_exam — mirroring gen_config.py:48-50 which already enforces push is not None and repeatability is not None.

## [MEDIUM] Show-ready state is not bound to the policy/motion identity — stale badge survives a policy swap
- **Where:** `pipeline/shows.py:166-181`
- **Scenario:** I tried to refute this and could not knock down the core defect — every mechanical claim checks out against the real code:

- shows.py:152-153 stores `dance.sim_exam = {"verdict","at","exam_id","metrics"}` with NO policy_sha256/motion_sha256. Confirmed.
- The §1 sim-run payload (contract docs 12-26) and the server endpoint (server.py:346-356) carry only passed/exam_id/metrics/video — no policy identity is captured. Confirmed.
- There is NO reset-on-change logic anywhere. Grep across pipeline/ and ui/ shows the only writers of `status`/`consecutive_clean` are record_sim_run and promote; neither
- **Fix:** In record_sim_run, persist policy_sha256/motion_sha256 (hashed from the files at dance.policy_path/motion_csv) into dance.sim_exam and into each repeatability history entry, and add policy_sha256 to the §1 sim-run payload/endpoint so runs are attributable. In promote() (shows.py:171-178), after the verdict/streak checks, re-hash the current file at dance.policy_path (and motion_csv) and refuse unless it equals the sha that produced the passing verdict and the clean streak. Add a supported update path (PATCH /api/dances/{id}) that, whenever policy_path or motion_csv changes, sets consecutive_cl

## [MEDIUM] Unlocked read-modify-write on the repeatability counter lets a concurrent pass mask a failing run
- **Where:** `pipeline/shows.py:140-163`
- **Scenario:** Confirmed at the code level. record_sim_run (pipeline/shows.py:140-163) does a load->mutate->save with no lock; the endpoint at ui/server.py:346 is a sync `def`, which FastAPI runs in a threadpool, so concurrent POSTs to the same dance run in parallel threads. Each request reloads the dance fresh from disk (_load_dance_or_404 -> load_dance, server.py:350), and _atomic_write (shows.py:70-73, os.replace) guarantees only last-writer-wins — it does not prevent lost updates. So the interleaving described (fail computes cc=0, concurrent stale pass computes cc=3 and saves last) is genuinely reachable
- **Fix:** Serialize per-dance mutations with a file lock (flock on dance.json) or a per-dance threading.Lock wrapping the load->mutate->save in record_sim_run, complete_step, promote, and record_outcome; or make these endpoints async and funnel all dance writes through a single worker. Additionally version/CAS the dance record so any fail is a hard reset that an older-generation read cannot overwrite.

## [MEDIUM] Pre-show checklist accepts a battery level below the documented do-not-start floor
- **Where:** `pipeline/shows.py:245-273`
- **Scenario:** I tried to refute this and could not. The code matches the finding exactly.

- The battery step's own detail text states the safety limit: "Below 30% do not start a show." (shows.py:56-57).
- complete_step (shows.py:245-273) is the only validation for that step. For kind=="number" it rejects bools (258-259), coerces to float (260-263), and enforces ONLY `0 <= value <= 100` (264-265). There is no floor check. `complete_step(show, "battery", 12)` records the step and returns normally.
- Once all five steps have entries, checklist_complete() (shows.py:218-219, via next_step 212-216) returns True 
- **Fix:** In complete_step, add a per-step floor for the battery step: after the 0-100 range check, if step=="battery" and value < BATTERY_FLOOR_PCT reject with a completion failure. Define BATTERY_FLOOR_PCT = 30 as a module constant and interpolate it into the step's detail text so the advisory text and the enforced limit cannot drift. Optionally also re-validate the battery value in show_deploy_gate as defense-in-depth before the hardware path goes live.

## [MEDIUM] Gantry (feet-off-ground) success is treated as license to go to ground, but validates none of the actual fall modes
- **Where:** `docs/ROBOT_DAY_RUNBOOK.md:68`
- **Scenario:** Verified against docs/ROBOT_DAY_RUNBOOK.md. Line 68 ("Repeat 3 clean runs before proceeding") gates Step 6 (feet-off-ground playback, lines 61-67) into Step 7 (ground). Step 6's watch/abort criteria are entirely joint-domain — "joint smoothness, no oscillation, no limit slamming; motor temps" (line 64) — with zero balance, GRF, foot-slip, or CoM checks, which is physically correct because in-air playback bears no weight. So the primary on-hardware evidence advancing to ground genuinely tells you nothing about ground balance; the only push-recovery certification is in sim (finding acknowledges 
- **Fix:** Insert an explicit low-authority ground-stability gate between Step 6 and Step 7: keep the safety line TAUT (partial-weight support), do a stand-and-hold with torque/tracking thresholds checked, then a slow first-motion still on the taut line, before releasing to the slack-line free ground playback on line 71-72. Also correct the runbook framing so 3 clean air-playback runs (line 68) are documented as a joint/thermal check only, not as evidence of ground balance.

## [LOW] Unsanitized --dance name interpolated into remote ssh/docker commands on the Jetson (command injection)
- **Where:** `deploy/10_gantry_test.sh:34`
- **Scenario:** CONFIRMED mechanically but IMPACT OVERSTATED. The injection is genuinely present and unmitigated: --dance is unvalidated at gen_config.py:58, 02_push_bundle.sh:19, and 10_gantry_test.sh:19; grep for any allowlist (^[, re.match, isalnum) across deploy/*.py, ui/server.py, pipeline/shows.py finds none. pc2 \"...${DANCE}...\" builds a single string with no local re-expansion, then lib.sh:33 does ssh ... \"$@\", so the remote Jetson shell parses metacharacters — a value like 'x; reboot #' executes on PC2 (10_gantry_test.sh:34,51-52; 02_push_bundle.sh:46). No mechanical guard prevents this.\n\nHowev
- **Fix:** Add a strict allowlist check (^[A-Za-z0-9_-]{1,64}$) on DANCE at the top of gen_config.py, 02_push_bundle.sh, and 10_gantry_test.sh, rejecting anything else. Quote remote interpolations and pass the name as a positional arg to a fixed remote script rather than string-building shell; confine the resolved bundle path under PC2_WORKDIR/bundles. Cheap defense-in-depth that also stops a mistyped ../ name from corrupting the -v mount into the privileged controller container.

## [LOW] Content integrity relies on 64-bit truncated sha256 throughout the artifact chain
- **Where:** `deploy/gen_config.py:34-35`
- **Scenario:** The code facts are 100% accurate and I confirmed all three cited sites. gen_config.py:35, sim_exam.py:477, and 02_push_bundle.sh:35 all call `hashlib.sha256(...).hexdigest()[:16]` — 16 hex chars = 64 bits. This truncated value is the ONLY cryptographic link across the whole chain: sim_exam writes it into the verdict (policy_sha256/motion_sha256, lines 535-537); gen_config recomputes it and gates the bundle on an exact string match against a passing verdict (find_passing_exam, lines 47-48); it is pinned into bundle.json (lines 91-94); and 02_push_bundle.sh recomputes+asserts it at push time. So
- **Fix:** Use full 64-hex sha256 for all artifact identity/matching. Change the shared helper(s) to return `hexdigest()` (no `[:16]`) in gen_config.py:35, sim_exam.py:477, and 02_push_bundle.sh:35, so the verdict, bundle manifest, and push-time re-check all store/compare full digests. Keep a truncated prefix only for human-facing display/log lines. No behavioral change to the gate logic beyond a wider string compare.
